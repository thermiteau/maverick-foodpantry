/* eslint-disable */

(function () {
  // check if not a valid character
  if (/[|;$%"<>{}+/]/.test(input)) {
    valid = 'Please do not use special characters e.g. not any of these | ; $ % " < >{ } + /';
    return;
  }

  valid = true;
})();
